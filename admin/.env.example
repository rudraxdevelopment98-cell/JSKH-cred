# Base URL for the JCred API. Leave as /api/v1 to use the Vite dev proxy.
VITE_API_BASE=/api/v1

# Dev proxy target (used by vite.config.js during `npm run dev`)
VITE_API_TARGET=http://localhost:4000
