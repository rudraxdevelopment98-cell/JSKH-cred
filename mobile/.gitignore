# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock
*.iml
.idea/
.vscode/

# The android/ project is committed so the APK build is reproducible
# (INTERNET permission, app name, etc.). Ignore only generated/local files.
android/.gradle/
android/.kotlin/
android/local.properties
android/key.properties
android/captures/
android/app/upload-keystore.jks

# Other platform folders are not committed yet; generate with `flutter create .`
ios/
web/
macos/
linux/
windows/
